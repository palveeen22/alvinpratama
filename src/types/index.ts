type TBlogs = {
	details: string
	title: string,
	date: string,
};

type TProjectCardProps = {
    project: {
        image: string
        url: string
        title: string
        description: string
        stacks: string[]
    }
}
